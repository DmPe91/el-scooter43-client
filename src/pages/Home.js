import React, { useEffect, useContext } from "react";
import { Context } from "..";
import Shop from "../components/Shop";

export const Home = () => {
  return (
    <>
      <Shop mt={3} />
    </>
  );
};
